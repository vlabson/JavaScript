

//                0       1       2       //
let alunos = ['Luiz','Maria','João']; // declaração de um array
alunos = 123;


/*
console.log(typeof alunos); // typeof retorna o tipo de dados do parametro
console.log( alunos instanceof Array);   // aqui o instanceof verifica se o tipo de dado é o mesmo que esta sendo passado no parametro. 
*/

//delete alunos[0]; // podemos deletar um dado mas dessa forma ficamos com um buraco no array.

/*alunos.shift(); // funcao para deletar/ remover no inicio do array
console.log(alunos);*/

//Alunos.pop(); // para deletar/ estourar um dado no final do array utiliza essa funcao POP // OBSEXISTE A POSSIBILIDADE DE SALVAR O DADO DELETADO EM UMA VARIAVEL TEMPORARIA;

/*alunos.unshift('luiza');
alunos.unshift('fabio');
console.log(alunos); // esta é uma forma de adcionar um novo dado no inicio do array usando a função UNSHIFT
*/

/*alunos.push('luiza');
alunos.push('fabio');
console.log(alunos); // esta é mais uma forma de inserir um novo dados no fim do array so que desta ves usando uma função
*/

/*alunos[alunos.length] = 'luiz';
alunos[alunos.length] = 'fabio';
alunos[alunos.length] = 'luana';*/ //esta é uma forma de inserir novos dados sempre no final do array 

/*alunos[0] = 'eduardo'; // para editar e alterar um valor no array faz dessa forma 
alunos[3] = 'Luiz' // para criar e incluir um novo campo e inserir dados faz dessa forma.*/

/*console.log(alunos); // aqui temos a impressão do array completo
console.log(alunos[0]); // nesses outros dois vamos imprimmir apenas o conteudo do indice especificado no colchete
console.log(alunos[2]);*/